
<?php
session_start();
require_once 'db_connect.php';

// Handle add to cart
if ($_SERVER['REQUEST_METHOD'] === 'POST' && isset($_POST['product_id'])) {
    $product_id = (int)$_POST['product_id'];
    if (!isset($_SESSION['cart'])) {
        $_SESSION['cart'] = [];
    }
    if (!isset($_SESSION['cart'][$product_id])) {
        $_SESSION['cart'][$product_id] = 1;
    } else {
        $_SESSION['cart'][$product_id]++;
    }
    header('Location: products.php');
    exit;
}

// Fetch products
$stmt = $pdo->query('SELECT product_id, name, price, description, quantity, image_url FROM products');
$products = $stmt->fetchAll();
?>
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Products</title>
    <style>
        .product-grid {
            display: flex;
            flex-wrap: wrap;
            gap: 24px;
        }
        .product-card {
            flex: 1 1 calc(33.333% - 24px);
            box-sizing: border-box;
            border: 1px solid #ccc;
            border-radius: 8px;
            padding: 16px;
            margin-bottom: 24px;
            background: #fafafa;
            min-width: 250px;
            max-width: 350px;
            display: flex;
            flex-direction: column;
            align-items: center;
        }
        .product-card img {
            max-width: 100%;
            max-height: 180px;
            object-fit: contain;
            margin-bottom: 12px;
        }
        .product-card h2 {
            font-size: 1.2em;
            margin: 0 0 8px 0;
        }
        .product-card .price {
            color: #007b00;
            font-weight: bold;
            margin-bottom: 8px;
        }
        .product-card .quantity {
            margin-bottom: 8px;
        }
        .product-card .actions {
            margin-top: auto;
        }
        @media (max-width: 900px) {
            .product-card { flex: 1 1 calc(50% - 24px); }
        }
        @media (max-width: 600px) {
            .product-card { flex: 1 1 100%; }
        }
    </style>
</head>
<body>
    <h1>Products</h1>
    <a href="cart.php">View Cart</a>
    <div class="product-grid">
        <?php foreach ($products as $product): ?>
            <div class="product-card">
                <a href="product_detail.php?id=<?= $product['product_id'] ?>">
                    <img src="<?= htmlspecialchars($product['image_url'] ?: 'placeholder.png') ?>" alt="<?= htmlspecialchars($product['name']) ?>">
                </a>
                <h2>
                    <a href="product_detail.php?id=<?= $product['product_id'] ?>">
                        <?= htmlspecialchars($product['name']) ?>
                    </a>
                </h2>
                <div class="price">$<?= number_format($product['price'], 2) ?></div>
                <div class="quantity">Quantity: <?= htmlspecialchars($product['quantity']) ?></div>
                <div class="actions">
                    <form method="post">
                        <input type="hidden" name="product_id" value="<?= $product['product_id'] ?>">
                        <button type="submit">Add to Cart</button>
                    </form>
                </div>
            </div>
        <?php endforeach; ?>
    </div>
</body>
?>
